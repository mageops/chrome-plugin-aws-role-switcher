{
    "name": "aws-role-switcher-chrome-extension",
    "private": true,
    "scripts": {
        "firefox-publish": "web-ext sign --channel=listed --api-key=$AMO_JWT_ISSUER --api-secret=$AMO_JWT_SECRET"
    },
    "devDependencies": {
        "web-ext": "^6",
        "dotenv": "^8"
    }
}
